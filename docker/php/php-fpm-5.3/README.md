# PHP-FPM 5.3

PHP 5.3 se ejecuta con una imagen comunitaria de compatibilidad. Antes de montar un `php.ini` propio, arranque el perfil y ejecute `php --ini` dentro del contenedor para confirmar la ruta válida de configuración de esa imagen.

No comparta configuraciones de PHP 5.3 con versiones modernas.
